import React from 'react';
import styles from './style.module.scss'

const DemoWrap = ({list}) => {
	console.log('DemoWrap list', list);
	return (
		<div className={styles.demoWrap}>
			
		</div>
	);
};

export default DemoWrap;
